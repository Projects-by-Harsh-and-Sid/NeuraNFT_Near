# TRON Private Network Configuration

localwitness = "{{SR_PRIVATE_KEY}}"

genesis.block = (
  witnesses = ["{{SR_ADDRESS}}"]
  Assets = [
    { accountName = "genesis", address = "{{SR_ADDRESS}}", balance = 1000000000000000 }
  ]
)

p2p {
  version = 11112
  listen.port = 9090
}

seed.node = []

# Other configurations as needed
